# spark-docker-local-cluster
Cluster Spark Standalone listo en Docker (master + workers + Jupyter), configurable vía .env y ejecutable con run.sh — perfecto para tests locales sin depender de Databricks ni Hadoop.
